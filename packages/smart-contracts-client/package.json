{
  "name": "smart-contracts-client",
  "version": "1.0.0",
  "description": "the client for getting and registering smart contracts to the smart contract api",
  "main": "./ts_build/index.js",
  "types": "./ts_build/index.d.ts",
  "scripts": {
    "compile": "./node_modules/.bin/tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Micah Riggan",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.9.4",
    "@types/web3": "^1.0.4",
    "typescript": "^3.0.3"
  },
  "dependencies": {
    "request": "^2.88.0",
    "request-promise": "^4.2.2",
    "decent-env-client": "1.0.0"
  }
}
