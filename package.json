{
  "name": "decent-services",
  "description": "A bundle of micro services for DecentVentures",
  "scripts": {
    "bootstrap": "./node_modules/.bin/lerna bootstrap",
    "compile": "./node_modules/.bin/lerna run compile",
    "start": "./node_modules/.bin/lerna run api:start --parallel"
  },
  "prettier": {
    "printWidth": 120,
    "singleQuote": true
  },
  "devDependencies": {
    "lerna": "^2.9.1",
    "prettier": "^1.14.2"
  }
}
